-- Store source for movies
ALTER TABLE `movie`
ADD `dateSearch` DATETIME NULL DEFAULT NULL,
ADD `searchCount` SMALLINT UNSIGNED NOT NULL,
ADD `identity` SMALLINT UNSIGNED NOT NULL,
ADD `quality` SMALLINT UNSIGNED NOT NULL,
ADD `score` SMALLINT UNSIGNED NOT NULL,
ADD `source` VARCHAR( 1024 ) CHARACTER SET utf8 COLLATE utf8_general_ci NULL;
