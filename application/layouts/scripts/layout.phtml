<?php echo $this->doctype() ?>
<html xmlns="http://www.w3.org/1999/xhtml"> 
<head>  
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" /> 
  
  <?php
  	echo $this->headTitle('mQueue')->setSeparator(' - '); 
  	echo $this->headLink(array('rel' => 'icon',
                                  'href' => $this->serverUrl() . $this->baseUrl('/images/favicon.png'),
                                  'PREPEND'))
  			->appendStylesheet($this->serverUrl() . $this->baseUrl('/css/both.css'), 'all')
  			->appendStylesheet($this->serverUrl() . $this->baseUrl('/css/local.css'), 'all')
  			->appendStylesheet($this->serverUrl() . $this->baseUrl('/css/gravatar.css'), 'all')
  			->appendStylesheet($this->serverUrl() . $this->baseUrl('/css/handheld.css'), 'handheld')
  			->appendStylesheet($this->serverUrl() . $this->baseUrl('/css/print.css'), 'print')
  			->appendAlternate($this->serverUrl() . $this->url(array('controller' => 'activity', 'action' => 'index', 'format' => 'atom'), null, true), 'application/rss+xml', $this->translate('mQueue - Overall activity'))
  			;
  
  	// Set localization file for timeago
	$lang = Zend_Registry::get('Zend_Locale')->getLanguage();
	$path = '/js/jquery.timeago.' . $lang . '.js';
	if (is_file(APPLICATION_PATH . '/../public' . $path))
	{
  		$this->headScript()->prependFile($this->serverUrl() . $this->baseUrl($path));
	}
  	
	echo $this->headScript()	
			->prependFile($this->serverUrl() . $this->baseUrl('/js/jquery.timeago.js'))
			->prependFile($this->serverUrl() . $this->baseUrl('/js/local.js'))
			->prependFile($this->serverUrl() . $this->baseUrl('/js/both.js'))
			->prependFile($this->serverUrl() . $this->baseUrl('/js/jquery-1.4.2.min.js'));
			
			
			
			
	echo $this->googleAnalytics();
	echo $this->googleWebmasterTools(); 
  ?>
</head> 
<body>
<div id="container">
	<div id="header">
		<?php echo $this->languageSelector(); ?>
		<div id="header-logo"><h1><a href="<?php echo $this->serverUrl() . $this->baseUrl(''); ?>" title="<?php echo $this->translate('mQueue monster !'); ?>"><span><?php echo $this->translate('mQueue'); ?></span></a></h1></div>
		<div id="header-navigation"><span>&nbsp;</span>
			<?php echo $this->navigation()->menu(); ?> 
			<?php echo $this->loginState(); ?> 
		</div>
	</div>
	<div id="content">
		<?php echo $this->flashMessenger(); ?>
		<?php echo $this->layout()->content; ?>
	</div>
	<div id="messages"><?php if (isset($this->messages)) foreach($this->messages as $message) echo '<p>' . $message . '</p>'; ?></div>
</div>
<div id="footer"><?php echo $this->footer(); ?></div>
</body>
</html>
