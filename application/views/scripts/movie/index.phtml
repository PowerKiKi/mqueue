

<h2></h2>Movies list</h2>
<table>
<th>Title</th>
<th>Status</th>
    <?php foreach ($this->entries as $entry): ?>
	<tr>
		<td><a href="
<?php echo $this->serverUrl() . $this->url(
    array(
        'controller' => 'status',
		'movie' => $entry->id
    ),
    'default', 
    true) ?>">*</a> <a href="<?php echo $entry->getImdbUrl(); ?>"><?php echo $this->escape($entry->getTitle()) ?></a></td>
		<td><iframe src="<?php echo $this->serverUrl() . $this->url(
    array(
        'controller' => 'status',
		'movie' => $entry->id
    ),
    'default', 
    true) ?>" >blop</iframe></td>
	</tr>
    <?php endforeach ?>
</table>

<p><a href="<?php echo $this->serverUrl() . $this->url(
    array(
        'controller' => 'movie',
        'action'     => 'add'
    ), 
    'default', 
    true) ?>">Add movie</a></p>